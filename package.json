{
  "name": "quickfire",
  "displayName": "quickFire",
  "description": "Execute VS Code commands and nested workflows instantly with a single keystroke—no Enter required.",
  "version": "0.0.3",
  "publisher": "dani84bs",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/dani84bs/quickfire.git"
  },
  "bugs": {
    "url": "https://github.com/dani84bs/quickfire/issues"
  },
  "homepage": "https://github.com/dani84bs/quickfire#readme",
  "license": "MIT",
  "keywords": [
    "quickpick",
    "workflow",
    "keyboard",
    "shortcuts",
    "productivity"
  ],
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "quickfire.showQuickActions",
        "title": "QuickFire: Show actions"
      }
    ],
    "configuration": {
      "title": "QuickFire",
      "properties": {
        "quickfire.actions": {
          "type": "array",
          "default": [],
          "description": "Configurable quick actions. Each item should have a 'key', 'description', and either a 'command' or an 'actions' array for a submenu.",
          "items": {
            "type": "object",
            "required": [
              "key",
              "description"
            ],
            "properties": {
              "key": {
                "type": "string",
                "description": "The key (letter) to trigger the action"
              },
              "command": {
                "type": "string",
                "description": "The command ID to execute"
              },
              "description": {
                "type": "string",
                "description": "A brief description of the action"
              },
              "args": {
                "type": [
                  "string",
                  "number",
                  "boolean",
                  "object",
                  "array",
                  "null"
                ],
                "description": "Optional arguments to pass to the command."
              },
              "actions": {
                "type": "array",
                "description": "A list of sub-actions. If this is provided, 'command' is ignored.",
                "items": {
                  "$ref": "#/properties/quickfire.actions/items"
                }
              }
            }
          }
        }
      }
    }
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "QuickFire executes configured commands. In untrusted workspaces, workspace-level QuickFire actions are disabled for security to prevent malicious code execution.",
      "restrictedConfigurations": [
        "quickfire.actions"
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test-compile": "tsc -p ./",
    "test": "npm run test-compile && vscode-test",
    "publish:open-vsx": "npx ovsx publish"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.107.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  }
}